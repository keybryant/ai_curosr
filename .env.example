# WebSocket: ws://<后端地址>/ws/ai-tool/<projectId>，无需登录与 token
# 后端地址（仅 host:port），如 localhost:8080
CURSOR_BACKEND_ADDRESS=localhost:8080
# 项目 ID（必填）
CURSOR_PROJECT_ID=your_project_id
# 若需写死完整 WS 地址可配置此项（一般不配）
CURSOR_WS_URL=

# Cursor 可执行文件。Windows 建议写完整路径，避免 PATH 未生效导致找不到
# 例如: CURSOR_EXE=D:\program\cursor\Cursor.exe
CURSOR_EXE=cursor
# 断线重连间隔（秒）
RECONNECT_INTERVAL=5
# 等待 Cursor 窗口/输入框 超时（秒）
CURSOR_UI_TIMEOUT=15
# 发送消息热键：Ctrl+Enter 或 Enter
CURSOR_SEND_HOTKEY=Ctrl+Enter
